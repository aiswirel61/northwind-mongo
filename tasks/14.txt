Create a SQL query to get Product list of stock is less than the quantity on order:

| ProductName | UnitsInOrder| UnitsInStock |

db.products.aggregate([
    { $project: {
        ProductName: 1,
        UnitsOnOrder: 1,
        UnitsInStock: 1, 
        check: {$gt: ["$UnitsOnOrder", "$UnitsInStock"]}
    }},
    { $match : { check: true }},
    { $project: { check: 0, _id: 0 }}
])