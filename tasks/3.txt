Create a query to return all customers from USA without Fax:

| CustomerId | CompanyName |

db.customers.aggregate([
    { $match : { Fax : "NULL" , Country: "USA" } },
    { $project: { _id: 0, CustomerID:1, CompanyName:1 } }
])